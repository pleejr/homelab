apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-certificate-production
  namespace: traefik
spec:
  secretName: traefik-cert-secret-production
  kind: ClusterIssuer
dnsNames:
  - traefik.local.pleejr.org