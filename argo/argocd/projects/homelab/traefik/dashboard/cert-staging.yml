apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-ingressroute-cert-staging
  namespace: traefik
spec:
  secretName: traefik-cert-secret-staging
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - traefik.pleejr.org
